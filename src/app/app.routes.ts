import { Routes } from '@angular/router';

export const routes: Routes = [
  // Example pattern for lazy-loaded features:
  // {
  //   path: 'users',
  //   loadChildren: () =>
  //     import('./features/users/users.routes').then(m => m.USER_ROUTES)
  // }
];
